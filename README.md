# USkill-Api
An agent trained in robotics simulation develops a complex grasp-and-sort skill over thousands of episodes. USKill extracts that skill as a portable Skill Object — a domain-agnostic JSON document encoding the skill's intent graph, primitive decomposition, and compatibility metadata. 
