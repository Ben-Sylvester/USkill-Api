[pytest]
asyncio_mode = auto
testpaths = tests
addopts =
    --tb=short
    -v
    --cov=app
    --cov-report=term-missing
    --cov-fail-under=69
filterwarnings =
    ignore::DeprecationWarning
    ignore::pytest.PytestUnraisableExceptionWarning
